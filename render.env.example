# Copy this to render.env and fill values, then add to Render dashboard env vars
GEMINI_API_KEY=your_gemini_api_key_here
JWT_SECRET=your-strong-jwt-secret
NODE_ENV=production
PORT=5001
CLIENT_ORIGIN=https://resume-analyzer-beta-rose.vercel.app

# Optional: local embedding model override
# HUGGINGFACE_EMBEDDING_MODEL=Xenova/all-MiniLM-L6-v2
